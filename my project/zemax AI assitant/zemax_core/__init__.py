"""
Zemax Copilot Core Engine Package
=================================
负责与 Ansys Zemax OpticStudio (ZOS-API) 的底层交互。
包含公差建模算法、敏感度分析引擎及 AI 优化策略。
"""

# 引擎版本 (对应 V6.0 Critical Fix)
__version__ = "6.0.0"

# -----------------------------------------------------------------------------
# 1. 暴露核心控制器
# -----------------------------------------------------------------------------
# Controller 是后端的唯一入口，外部不需要直接调用 services_xxx 模块
from .controller import ZemaxController

# -----------------------------------------------------------------------------
# 2. 暴露关键配置常量 (可选)
# -----------------------------------------------------------------------------
# 允许上层应用读取当前的 Zemax 路径和公差标准
from .config import ZEMAX_PATH, Q_GRADES

# -----------------------------------------------------------------------------
# 3. 定义导出白名单
# -----------------------------------------------------------------------------
# 保持命名空间整洁，隐藏 services_ai, services_tol 等具体实现细节
__all__ = [
    "ZemaxController",
    "ZEMAX_PATH",
    "Q_GRADES",
    "__version__"
]